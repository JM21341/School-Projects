#include <stdio.h>
#include "counter.h"

int main(){
    int auto localVariable;
    int register registerVariable;
    incrementCounter();
    incrementCounter();
    incrementCounter();
    incrementCounter();
    incrementCounter();
}